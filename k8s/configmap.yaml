# 秘密でない設定。秘密は SealedSecret（sealed-secret.example.yaml）へ。
apiVersion: v1
kind: ConfigMap
metadata:
  name: hoshid-config
  namespace: hoshid
data:
  # **issuer になる値。** ここを変えると、繋がっている RP の設定を全て
  # 変えることになる。RP に渡す issuer は末尾に /api/auth が付いた
  # https://hoshid.skyia.jp/api/auth であって、この値そのものではない。
  BETTER_AUTH_URL: "https://hoshid.reeldev.jp"

 BETTER_AUTH_TRUSTED_ORIGINS: "https://hoshid.reeldev.jp"

  TRUSTED_PROXIES: "10.1.0.0/16"
  NODE_ENV: "production"
  NEXT_TELEMETRY_DISABLED: "1"

  # **Traefik の Pod が属する CIDR。** MicroK8s の既定の Pod CIDR
  # （Calico の 10.1.0.0/16）。k3s は 10.42.0.0/16 で別物なので、
  # 移すときは必ず確認する。
  #
  #   kubectl get pod -A -l app.kubernetes.io/name=traefik -o wide
  #
  # ここがずれると、クライアント IP を特定できずレート制限が全員で1つの
  # バケツになる（ログインは10秒3回）。**気づける形では壊れない。**
  # デプロイ後、監査ログの IP 欄が埋まっているかで答え合わせできる。
  #
  # プライベート帯を丸ごと書かないこと。そこに利用者が含まれると、
  # 詐称されたヘッダを信じることになって設定の意味が消える。
  TRUSTED_PROXIES: "10.1.0.0/16"

  NODE_ENV: "production"
  NEXT_TELEMETRY_DISABLED: "1"

  # さくらのメールボックス。587 は STARTTLS なので SECURE は false。
  # SMTP_USER と SMTP_PASS は対で Secret に入れる。
  SMTP_HOST: "smtp.gmail.com"
  SMTP_PORT: "587"
  SMTP_SECURE: "false"
  MAIL_FROM: "HoshID <rielu1227@gmail.com>"

  # Discord のロール付与。ID は秘密ではない（Bot トークンだけが秘密）。
  DISCORD_GUILD_ID: "1551668897395118221"
  DISCORD_MEMBER_ROLE_ID: "1551670153123471460"
  DISCORD_GUILD_INVITE_URL: "https://discord.gg/wfSx4RnSgP"
